<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.ghost.games.numberhero.activity"
      android:versionCode="106140221"
      android:versionName="1.0.6.140221">
    <uses-sdk android:minSdkVersion="8" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>

    <application android:icon="@drawable/ico" android:label="@string/app_name">
    	 <!-- 下面是测试的配置 -->
    	 <uses-library android:name="android.test.runner"/>
    	  <!-- 上面是测试的配置 -->
    	 <activity android:name=".LoginActivity"
                  android:label="@string/app_name"
                  android:screenOrientation="portrait"
                  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateUserActivity"
                  android:label="@string/app_name"
                  android:screenOrientation="portrait">
        </activity>
        <activity android:name=".NumberHeroActivity"
                  android:label="@string/app_name"
                  android:screenOrientation="portrait"
                  >
        </activity>
		<activity android:name=".ResultActivity"
                  android:label="@string/app_name"
                  android:screenOrientation="portrait">
        </activity>
    	<!--<uses-library android:name="android.test.runner"/> -->
   	</application>
    <!-- 下面是测试的配置 -->
    <instrumentation 
    	android:name="android.test.InstrumentationTestRunner"
    	android:targetPackage="com.ghost.games.numberhero.activity"
    	android:label="Testing"
    	>
    	</instrumentation>
     <!-- 上面是测试的配置 -->	
    </manifest>